---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---
{date.getFullYear() == 9999 ? 'Present': 
<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: '2-digit',
			timeZone: 'UTC',
		})
	}
</time>
}
